-- Solus Conky Theme Settings
-- Edit these values to customize your theme

local settings = {
    --------------------------------------------------------------------------------
    -- Dimensions (base dimensions, will be multiplied by scale)
    --------------------------------------------------------------------------------
    width = 1366,
    height = 748,

    --------------------------------------------------------------------------------
    -- Network Interface (e.g., wlo1, wlan0, enp3s0, eth0)
    --------------------------------------------------------------------------------
    network_interface = "wlo1",

    --------------------------------------------------------------------------------
    -- CPU Cores (for future use)
    --------------------------------------------------------------------------------
    no_of_cores = 4,

    --------------------------------------------------------------------------------
    -- Theme Mode: "DARK" or "WHITE"
    --------------------------------------------------------------------------------
    theme_mode = "DARK",

    --------------------------------------------------------------------------------
    -- Scaling Factor
    -- 1.0 = 1080p (Full HD)
    -- 1.5 = 1440p (2K / QHD)
    -- 2.0 = 2160p (4K / UHD)
    --------------------------------------------------------------------------------
    scale = 1.0,

    --------------------------------------------------------------------------------
    -- Display Server Settings
    --------------------------------------------------------------------------------
    -- Force X11 mode (required for Lua ring graphics)
    -- Set to true (default) for full ring graphics via XWayland
    -- Set to false for native Wayland (text only, no rings)
    force_x11 = true,
}

return settings
